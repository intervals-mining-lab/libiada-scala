package org.foarlab

import org.scalatest.FunSpec

/**
 * Created by goruha on 23.09.15.
 */
/*class LinkSpec extends FunSpec {
  describe("Link") {
    describe("End") {
      it("should have 1 for neighbor elements") {
        val sequence: List[(Int, String)] = List( (1, "A") , (2, "A"))
        assertResult(1)(Link.End[String](sequence)((1, "A"))._1)
      }

      it("should have 2 for elements") {
        val sequence: List[(Int, String)] = List( (1, "A") , (2, "B"), (3, "A"))
        assertResult(2)(Link.End[String](sequence)((1, "A"))._1)
      }

      it("should have 1 for last A element") {
        val sequence: List[(Int, String)] = List( (1, "A") , (2, "B"), (3, "A"))
        assertResult(1)(Link.End[String](sequence)((3, "A"))._1)
      }

      it("should have 2 for last B element") {
        val sequence: List[(Int, String)] = List( (1, "A") , (2, "B"), (3, "A"))
        assertResult(1)(Link.End[String](sequence)((2, "B"))._1)
      }
    }
  }
}*/
